import tailwindcss from 'tailwindcss';
import autoprefixer from 'autoprefixer';
import tailwindConfig from './tailwind.config.js'; // Ensure correct path

export default {
    plugins: [
        tailwindcss(tailwindConfig),
        autoprefixer,
    ],
};